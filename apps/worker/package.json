{
  "name": "@cag/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Polling, indexing, reconciliation, and projection worker.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@cag/config": "workspace:*",
    "@cag/db": "workspace:*"
  }
}
