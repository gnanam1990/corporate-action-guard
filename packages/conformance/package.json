{
  "name": "@cag/conformance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "B20 integration conformance scenarios, adapter contract, mutation corpus, and CI-neutral reports.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@cag/domain": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.9.0"
  }
}
