# Corporate Action Guard — local environment template.
# Copy to .env and fill in. NEVER commit a filled .env.
#
# Anything prefixed NEXT_PUBLIC_ is compiled into the browser bundle.
# No server secret may ever use that prefix (enforced by packages/config and by a
# bundle scan in `pnpm --filter @cag/web build` verification).

NODE_ENV=development

# --- Database -------------------------------------------------------------
DATABASE_URL=postgresql://guard:guard@localhost:55432/guard

# --- xStocks production API ----------------------------------------------
# Verified 2026-09-03: the /api/v2 path is required; the bare host returns 404.
XSTOCKS_API_BASE_URL=https://api.xstocks.fi/api/v2

# --- X Layer -------------------------------------------------------------
# Mainnet is READ ONLY. There is no signing code path for chain 196.
XLAYER_MAINNET_RPC_URL=
XLAYER_TESTNET_RPC_URL=
XLAYER_MAINNET_CHAIN_ID=196
XLAYER_TESTNET_CHAIN_ID=1952

# --- Service URLs --------------------------------------------------------
API_PUBLIC_BASE_URL=http://localhost:4000
# Optional server-side override. Docker Compose sets this to http://api:4000; leave it
# unset when running the API and Next.js directly on the host.
# API_INTERNAL_BASE_URL=http://api:4000
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# --- Operator authentication ---------------------------------------------
# Hash only. Raw API keys are never stored or logged.
OPERATOR_API_KEY_HASH=
INTEGRATOR_API_KEY_HASH=
FIXTURE_API_KEY_HASH=
# Raw local client credential used only by `pnpm demo:testnet`; never inject it into the
# API, worker, or web containers. `pnpm testnet:init` creates a matching ignored pair.
INTEGRATOR_API_KEY=

# Development-only API keys, hashed at startup and never retained in plaintext.
# Format: keyId|principal|scope[,scope]|rawKey   (entries separated by ';')
# Fields use '|' because scope names contain a colon.
# Quote the value: the '|' separators are shell pipes when the file is `source`d.
# DEV_API_KEYS='integ001|integrator-a|integrator:preflight|cag_integ001_0123456789abcdef0123456789abcdef'

# --- Receipt signer (testnet only) ---------------------------------------
# Production API startup requires aws-kms mode. The local private-key mode is rejected
# under NODE_ENV=production.
RECEIPT_SIGNER_MODE=local
RECEIPT_SIGNER_PRIVATE_KEY=
RECEIPT_SIGNER_ADDRESS=
AWS_KMS_KEY_ID=
AWS_REGION=

# --- Deployed testnet contracts (chain 1952) ------------------------------
# Populated only from a verified deployment artifact; never hand-written.
TESTNET_DEPLOYER_PRIVATE_KEY=
GUARD_ADAPTER_TESTNET_ADDRESS=
GUARD_ADAPTER_DEPLOYED_AT_BLOCK=
PROTECTED_VAULT_TESTNET_ADDRESS=
FIXTURE_ASSET_TESTNET_ADDRESS=
FIXTURE_WRAPPER_TESTNET_ADDRESS=
FIXTURE_ADMIN_ADDRESS=
FIXTURE_ASSET_ID=CAG-FIXTURE

# --- Signed fixture intent publisher (operator workstation only) ---------
# These raw credentials are consumed only by `pnpm fixture:evidence:publish`; they must
# never be injected into the API, worker, web, or migration containers.
FIXTURE_ADMIN_PRIVATE_KEY=
FIXTURE_API_KEY=
FIXTURE_MULTIPLIER_VALUE=
FIXTURE_MULTIPLIER_DECIMALS=
FIXTURE_MULTIPLIER_NONCE=
# RFC 3339 timestamp, or the literal `none`.
FIXTURE_SCHEDULED_ACTIVATION=none

# --- Base B20 Equity Integrity Layer --------------------------------------
# Every variable below is OPTIONAL. An X Layer deployment starts unchanged with all of
# them unset. A feature never becomes enabled merely because an address string exists.

# Base mainnet is READ-ONLY (ADR 0007). There is deliberately no signer variable for it,
# and no code path that could use one.
BASE_MAINNET_CHAIN_ID=8453
BASE_MAINNET_RPC_URL=
BASE_CONFIRMATIONS=
BASE_REORG_LOOKBACK_BLOCKS=

# Base Sepolia writes stay disabled unless the flag, a signer provider, the observed
# chain id, a funded balance and a verified capability are ALL present.
BASE_SEPOLIA_CHAIN_ID=84532
BASE_SEPOLIA_RPC_URL=
BASE_SEPOLIA_WRITES_ENABLED=false
BASE_SEPOLIA_DEPLOYER_PRIVATE_KEY=

# Reviewed provenance artifacts. Startup fails if an enabled feature names a manifest
# that is missing, malformed, or recorded for the wrong chain.
B20_OFFICIAL_ASSET_MANIFEST_PATH=provenance/base-b20/asset-manifest.json
B20_CAPABILITY_MATRIX_PATH=provenance/base-b20/capability-matrix.json
CHAINLINK_FEED_MANIFEST_PATH=provenance/base-b20/feed-manifest.json
B20_TOKEN_FEED_PAIRING_PATH=provenance/base-b20/token-feed-pairing.json

# Ingestion bounds. A provider's log-range limit is discovered and adapted to, not guessed.
B20_MAX_EVENT_RANGE=
B20_POLL_INTERVAL_MS=

# Freshness is per action class, never one universal timeout (ADR 0006, invariant V5).
CHAINLINK_SEQUENCER_FEED_ADDRESS=
CHAINLINK_DEFAULT_STALE_AFTER_SECONDS=
CHAINLINK_SEQUENCER_GRACE_SECONDS=

# Populated only from a verified Base Sepolia deployment artifact; never hand-written.
B20_GUARD_ADAPTER_SEPOLIA_ADDRESS=
B20_PROTECTED_VAULT_SEPOLIA_ADDRESS=
B20_FIXTURE_SEPOLIA_ADDRESS=

# Feature flags. Default off: a half-built Base surface must not appear in an X Layer
# deployment that upgraded for an unrelated reason.
B20_READS_ENABLED=false
B20_LEDGER_ENABLED=false
B20_PREFLIGHT_ENABLED=false
B20_ADAPTER_ENABLED=false

# Optional monetization for premium read-only artifacts. Never gates a safety decision.
X402_ENABLED=false
X402_FACILITATOR_URL=
X402_PAY_TO_ADDRESS=
