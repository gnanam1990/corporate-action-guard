global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: corporate-action-guard-api
    metrics_path: /metrics
    static_configs:
      - targets: ['api:4000']
